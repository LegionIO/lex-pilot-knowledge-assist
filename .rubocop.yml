inherit_gem:
  rubocop-legion: config/lex.yml

# Private helper methods return scalars (confidence, arrays) — not public runner return values
Legion/Extension/RunnerReturnHash:
  Enabled: false

# Runners use include/mixin pattern — extend self not needed
Legion/Extension/RunnerIncludeHelpers:
  Enabled: false
